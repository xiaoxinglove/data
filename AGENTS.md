# 项目协作规则

## 项目事实与证据

- 当前项目是 Python / FastAPI 骨架；真实能力以代码及可复验结果为准。
- 不把示例、规划、占位返回或目标架构写成已完成事实。
- 结论区分已验证事实、合理推断与建议。进度不得使用没有依据的完成百分比。
- 先理解相关代码、调用方和接口，再采用满足需求的最小方案。
- 重要且有争议的技术决策检查正反论据、边界和失败模式；简单事实无需机械辩论。
- 外部技术版本、标准与不确定结论查阅最新一手资料；仓库事实以本地证据为准。不得上传密钥或私有代码用于搜索。
- 仅在答案影响范围、安全或验收时提问，每轮最多三个；已有信息足够时直接执行。

## 任务范围与工作流程

- 用户当前请求优先于进度文件中的“下一步”；“下一步”不自动授权其他工作。
- 只读咨询不修改文件、不提交。已授权的常规可逆修改直接执行；超出范围或不可逆操作再确认。
- 开发前检查 Git 状态，读取根进度、相关决策和受影响模块文档，保留用户已有改动。
- 同时只有一个主交付目标；其必要依赖、测试和文档修改属于同一目标。
- 收尾更新根 PROGRESS.md 和受影响模块 PROGRESS.md；未受影响模块不机械修改日期。
- 修改任务的适用检查通过后，默认进行本地提交；只读任务不提交，不自动推送远端。
- 提交前逐项暂存并检查差异，仅包含本任务涉及的文件。首次提交也不得夹带已有用户代码、密钥或无关文件。
- 无法提交时记录具体原因；提交成功不代表业务验收通过。

## 验证与完成定义

- 业务功能按单元、集成、端到端顺序验收；上一层失败，不进入下一层验收。
- 启动成功、HTTP 请求成功、真实业务验收通过是不同结果。空返回或模拟依赖不能证明真实 RAG 或外部服务完成。
- 文档变更检查路径、命令、接口和代码的一致性。某层不适用须写明原因；缺环境、缺测试属于阻塞。
- 基线已失败时记录原有失败，允许修复相关基础设施，不得伪造通过。
- Python 代码变更交付要求 pytest、mypy --strict、ruff check；具体范围与顺序见 docs/testing-standards.md。
- 执行仓库已实现的验证入口；入口缺失时记录阻塞，不编造命令或通过结果。当前状态只在 PROGRESS.md 维护。

## 安全与数据

- 目标要求：业务 API 默认拒绝匿名访问，验证可信访问令牌及权限；用户身份认证采用适合部署的 OIDC 等身份方案。
- 匿名端点必须显式列出用途、输出限制和测试依据；例外清单见 docs/api-patterns.md，现有实现差距见 PROGRESS.md。
- 渠道回调按官方协议验签并防重放；安全占位不能恒真放行。
- Security 制定权限策略；RAG、Memory、MCP 在读取或执行位置落实可信用户、租户和资源范围，缺少上下文时默认拒绝。
- 关系数据库使用 SQLAlchemy 2.0 风格接口、参数化查询和明确事务。其他存储使用对应客户端，见 docs/database-rules.md。
- 密码、令牌和密钥不得写入日志、文档、Git 或长期记忆。

## 文档与代码同步

- PROGRESS.md 负责维护代码、配置、测试和文档的同步台账，是同步工作的唯一记录入口。
- 每次修改在 PROGRESS.md 记录：变更代码、受影响文件、已同步文档、验证依据、结果和未同步原因。
- README.md 是稳定的项目总说明手册；只有项目定位、架构、接口、依赖、配置、运行方式、安全边界或路线发生变化时才更新。
- 模块细节同步到对应 ARCHITECTURE.md 与模块 PROGRESS.md；API、数据库和测试约束同步到相应专题文档。
- 新命令实际验证后才能写成可用；规划能力必须与当前实现明确分开。
- 纯内部改动不影响任何说明文档时，也要在根 PROGRESS.md 记录判断依据。
- 这是一套人工维护流程，不表示仓库已有自动文档生成或完整 CI 同步检查。

## 文档职责与导航

- AGENTS.md：项目总则与导航；专题规范补充细则。规则冲突时显式说明，按用户当前指示解决，不静默选择。
- PROGRESS.md：项目当前状态、验证证据、阻塞、下一步及文档代码同步台账。
- DECISIONS.md：已采纳决策、理由、替代方案和约束。
- README.md：当前能力、环境和使用说明。
- Initialization.md：环境基线；plan.md：当前交付计划。
- docs/features.md：功能与验收；人工维护需求，验证器维护通过结果；验证器建立与自测纳入 F001，在此之前不得标 passing。
- app/*/ARCHITECTURE.md：当前接口与明确标注的目标边界。
- app/*/PROGRESS.md：模块事实、验证、限制和下一步。
- 添加或修改 API 读 docs/api-patterns.md；存储变更读 docs/database-rules.md；测试变更读 docs/testing-standards.md。
- 所有路径相对仓库根目录；统一使用 AGENTS.md、PROGRESS.md、README.md 的大小写。
