# 设计决策

## 2026-09-20：文档以代码与验证证据为基线

- 状态：已采纳。
- 决策：AGENTS.md 维护规则；根和模块 PROGRESS.md 维护事实；README.md 随用户可见的代码行为同步。
- 原因：原文档混入未实现系统、示例提交和测试成绩，无法作为交接依据。
- 约束：没有实际验证不得写“通过”；目标设计必须标为待实现。
- 验证：本轮文档一致性检查，结果见 PROGRESS.md。

## 已移除的示例

原 2024-01-15 Redis 用户偏好缓存条目没有当前代码或需求证据，不作为已采纳决策。
数据库、缓存、向量库、模型和身份提供方尚未选定；后续按真实需求记录决策、替代方案和代价。

## 2026-09-20：规则分层与验证归属

- 状态：已采纳（本轮完善）。
- 决策：AGENTS.md 为总则，专题规范为细则；环境基线集中记录于 PROGRESS.md，README 保留用户使用所需事实。
- 验证结果采用自动生成方案，验证器及自测属于 F001；未采用临时人工标 passing，以避免后续迁移和证据标准不一致。
- README 采用同次变更维护加收尾语义核对；暂不构建自动生成系统，结构检查无法替代语义一致性。

## 2026-09-20：F001 使用本地 OAuth 2.0 客户端凭据

- 状态：已采纳，仅用于 MVP。
- 决策：令牌端点通过 HTTP Basic 验证单个机密客户端，接受 client_credentials，签发 15 分钟 HS256 JWT。
- 权限：health:read、documents:write、chat:use、enterprise:write；资源端验证 issuer、audience、expiry 和 scope。
- 理由：当前是机器到机器的后端骨架，没有用户、浏览器登录或外部身份提供方；客户端凭据是最小闭环，避免使用密码授权。
- 反方与边界：本地共享密钥缺少集中撤销、轮换、多客户端治理和用户身份，不能直接用于生产。生产化时迁移到独立 OAuth/OIDC 提供方及非对称签名。
