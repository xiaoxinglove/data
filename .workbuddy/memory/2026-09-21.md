# 2026-09-21 工作日志

## 诊断：F001 完整验证链实测（只读，未修改仓库）

用户问「项目还能完整运行吗」。实测结论：**不能运行**，工作区处于 `git mv app backend` 的半途重构态。

- `app/` 目录已整体删除（git status 显示 30 个 `D` 条目），`backend/` 为等价副本但**未接线**。
- 探针验证（临时 shim 把 `sys.modules['app'] = backend`）：`IMPORT_OK`，路由 5 条与 README 一致
  → 证明修复只需改 import 前缀，不是结构性损坏。
- 分层实测：`uv sync --locked` 通过（29 包）、`ruff check .` 通过；
  `mypy` ❌ `cannot read file 'app'`；`pytest tests/unit|integration` ❌ `No module named 'app'`；
  `uvicorn app.main:app` 与 `python backend/main.py` 均 ❌。
  `scripts/check.py` 在 mypy 步抛 `CalledProcessError` 中断。

### 待修清单（import 8 处 + 配置/脚本 5 处）
- 源码：`backend/main.py:13-15`、`backend/llm.py:9`、`backend/agent/kernel.py:1-3`
- 配置/脚本：`pyproject.toml:31`（mypy files 含 `app`）、`run.ps1:24`、
  `scripts/http_check.py:144`、`tests/conftest.py:22-23`、`tests/unit/test_mvp.py:8,9,27,28,42,43`、
  `tests/integration/test_api.py:47`

### 环境坑（本机特有，别重复踩）
- `.venv` 是从 `...\重做\data\.venv` 搬来的（`pyvenv.cfg` 的 `command` 字段为证），
  console script trampoline 失效 → `uv run pytest` / `uv run uvicorn` 报
  `uv trampoline failed to canonicalize script path`。**必须删掉 .venv 用 `uv sync` 重建**，
  或统一改用 `uv run python -m <mod>` 形式绕过。
- `[project] name = "data"` + `src/data/`（`uv init` 样板，内容仍是 `print("Hello from data!")`）；
  `uv_build` 只打包 `src/data`，**`backend/` 不在打包范围内**，装到别处会 import 失败。
  这是部署隐患，不只是 import 改名问题。
- `tests/evaluation/` 仅 `.gitkeep`；`check.py` 不覆盖它。
- `deploy/` 只有 prometheus/loki/grafana，无后端服务定义。
- `frontend/` 无 `node_modules`，需 `npm install`。
- `.check_data/F001.json` 的 `status: passing` 归属旧提交 `ce77708`（9-20），当前结构下无效。
- 文档与事实脱节：`README.md`、`PROGRESS.md` 声称验证通过，与实际全失败矛盾，
  违反 `AGENTS.md`「不把规划写成已完成事实」。**未修改文档，等用户决定方向。**

---

## 修复：完成 app → backend 重命名（用户选择保留 backend 命名）

已提交 `f1eadd9`。工作区现已全绿。

### 一个必须记住的判断
用户最初要求「在脚本当中加上 sys.path」来解决 `No module named 'fastapi'`。
**这个方案无效**，已实测证伪并纠正：

- 实测 `sys.path.insert(0, 项目根)` 后 `import fastapi` 依然失败 →
  sys.path 只决定自研模块查找目录，管不到第三方包的 site-packages。
- `backend/main.py` 本来就已有 sys.path 逻辑。
- 真正的失败是三层叠加：①解释器错（PATH 上的 3.13.12 无 fastapi）→
  ②包名错（`app` 已不存在）→ ③`.venv` 搬迁导致 console script trampoline 失效。
- 项目 `requires-python >=3.14`，PATH 上是 3.13.14，本身就不在支持范围。

### 实际改动
- 源码 import 前缀：`backend/main.py:14-16`、`backend/llm.py:9`、
  `backend/agent/kernel.py:1-3`、`backend/api/router.py` 文档串
- `backend/main.py` 的 `ROOT_DIR`：用户改成的 `parents[2] / "src"`
  （越界到 `...\重做\src`，目录不存在）→ 修正为 `parents[1]`
- 配置/脚本：`pyproject.toml` mypy files、`run.ps1`、`scripts/http_check.py`
- 测试：`tests/conftest.py`、`unit/test_mvp.py`、`integration/test_api.py`、
  `integration/test_direct_entrypoint.py`（含冲突包回归测试的 `app` → `backend`）
- 文档：14 个 md 批量替换 + README 结构树重写 + PROGRESS 台账新增一行
- 删除并 `uv sync` 重建 `.venv`（原 venv 的 `pyvenv.cfg command` 指向
  `...\重做\data\.venv`，是搬过来的）

### 验证结论（实测）
`uv sync --locked` / ruff / mypy 23 文件 / 单元 4 / 集成 6 / 安全 11 /
http_check / `scripts/check.py` 全通过。真实服务实测：无令牌 401、
带令牌 `/health` 200、写资料 201、缺 `GLM_API_KEY` 时 `/chat` 502（符合 README）。
`F001.json` 记录 `f1eadd9`，与 HEAD 一致。

### 仍然存在、未解决的（已写入 PROGRESS「当前限制」）
- `pyproject.toml` 项目名仍是 `uv init` 默认值 `data`，`src/data/` 是无人引用的样板包；
  uv_build 只打包 `src/data`，**`backend/` 不在打包范围内**，现在靠工作目录进
  sys.path 才能 import。装到项目目录之外会失败 —— 这是下一个该处理的结构问题。
- `backend/core/`、`backend/observability/` 是完全空目录（无 `.gitkeep`，Git 不跟踪）。
- `tests/evaluation/` 只有 `.gitkeep`，`check.py` 不覆盖它。
- `frontend/`（Vue+Vite 骨架，无 node_modules，未调后端）、`deploy/`（仅 Prometheus/Loki
  配置）、`.workbuddy/` 均未纳入本次提交，保持未跟踪。

