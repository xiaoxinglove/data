# 项目进度

更新日期：2026-09-20

## 本轮交付：文档完善

已完成规则分层、README 同步记录要求、F001 目标契约和验证器职责定义，清理模块架构中的历史接口说明。
本轮仅修改文档，业务代码、依赖和功能状态没有改变。

## 本轮文档验收

| 检查 | 命令或可复验步骤 | 结果 |
| --- | --- | --- |
| 文档结构 | rg --files -g '*.md' 列出文件；逐一检查非空及行首三个反引号数量为偶数 | 27 份非空，代码围栏配对，通过 |
| 必需路径 | Test-Path 检查根 7 份文档和 docs 下 4 份规范 | 11 个路径存在，通过 |
| 路由一致性 | rg -n '@(app\|router)\.(get\|post)' app；与 README 的三个路由逐项核对 | 三个显式业务路由一致，通过 |
| 语义核对 | 阅读规则总则、专题规范、功能清单与计划；区分目标行为和当前行为 | F001 明确标为待实现；README 保持当前占位行为 |
| 单元 / 集成 / 端到端 | 本轮仅文档变更 | 均不适用：未修改业务代码；不代表业务验收通过 |

适用范围：本轮文档工作区；检查日期 2026-09-20。没有声称已经校验所有任意文本路径或外部链接。

## README 同步记录

- 触发变化：规则增加可追溯的 README 收尾核对要求。
- 对应段落：README.md 的“文档同步”。
- 核对依据：app/main.py、app/api/router.py、app/agent/kernel.py、pyproject.toml 和 src/data/__init__.py。
- 结果：入口、三条显式业务路由、空检索返回、包版本及依赖缺口与当前代码一致。
- F001 计划中的 422、401、403、501、not_implemented 和启动命令仅属于待实现契约，没有写成 README 当前能力。

## 项目业务验证基线（独立于本轮文档验收）

- 源码为 11 个 Python 文件，约 74 行；没有测试套件。
- FastAPI 未声明为项目依赖，缺少 uv.lock、ASGI 服务和开发检查工具配置。
- 本轮 make check：退出码 1，make 不在 PATH；仓库也无 Makefile。
- 先前 uv lock --check：失败，缺少 uv.lock。本轮未重复执行。
- 业务单元、集成和端到端均未通过验收；不因文档检查通过而提升状态。
- API 未接入认证；RBAC 和渠道校验恒真；真实 RAG、Memory、MCP 和评估仍未实现。

## Git 交付范围

此次本地提交仅纳入 27 份已审阅的 Markdown 文档，记录为文档基线；已有 Python 代码、配置、.env 和其他文件不纳入本次提交。
文档基线提交不是可运行项目发布；已有源码继续保留在工作区。提交结果由 git log 核查，避免将本提交自身哈希写入造成循环修改。

## 下一步

F001 待开发，按 docs/features.md 的目标契约实现依赖、启动、安全边界及验证器。业务验收顺序保持单元 → 集成 → 端到端。
